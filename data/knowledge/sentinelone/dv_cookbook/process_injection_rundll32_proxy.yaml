template_id: process_injection_rundll32_proxy
hunt_pattern: process_injection
mitre:
- tactic: Defense Evasion
  technique_id: T1055
  technique_name: Process Injection
query_source:
  natural_language_prompt: Show me rundll32 process executions
  resulting_query: event.category = "process" and process.name contains "rundll32" | columns event.time,
    endpoint.name, process.name, process.cmdline | limit 5
  purple_ai_status: purple_ai() failed systemically this session (confirmed via diagnostic probe, not
    a phrasing issue -- see field_dictionary.yaml). This query is hand-composed from confirmed field_dictionary.yaml
    fields as a fallback, per explicit user direction, and has NOT been generated or verified by purple_ai()
    the documented way.
  confidence_note: 'Weakest template in this set: no cross-process/API-call field was confirmed (e.g.
    a target-process or memory-write field), so this only proxies via a commonly-abused LOLBin name, not
    true injection telemetry.'
default_window: PT24H
result_cap: 5
false_positives_observed: []
triage_status: not independently triaged this session -- ran_without_error and result_sample below are
  the only evidence checked
status: experimental
validation_result:
  ran_without_error: true
  error: null
  result_sample: ''
