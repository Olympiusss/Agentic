template_id: lateral_movement_psexec_proxy
hunt_pattern: lateral_movement
mitre:
- tactic: Lateral Movement
  technique_id: T1021.002
  technique_name: SMB/Windows Admin Shares
query_source:
  natural_language_prompt: Show me process command lines referencing psexec
  resulting_query: event.category = "process" and process.cmdline contains "psexec" | columns event.time,
    endpoint.name, process.name, process.cmdline | limit 5
  purple_ai_status: purple_ai() failed systemically this session (confirmed via diagnostic probe, not
    a phrasing issue -- see field_dictionary.yaml). This query is hand-composed from confirmed field_dictionary.yaml
    fields as a fallback, per explicit user direction, and has NOT been generated or verified by purple_ai()
    the documented way.
  confidence_note: Coarse proxy via a known lateral-movement tool name in the command line; no network-category
    fields were confirmed (network category returned no data in the Milestone 1/5 probe window), so a
    connection-based lateral-movement signal isn't available here.
default_window: PT24H
result_cap: 5
false_positives_observed: []
triage_status: not independently triaged this session -- ran_without_error and result_sample below are
  the only evidence checked
status: experimental
validation_result:
  ran_without_error: true
  error: null
  result_sample: ''
