# Ontology (Milestone 2 — populated, draft pending review)

Generated by `scripts/build_sentinelone_ontology.py` from a live 25-item sample per core entity plus `../environment_map.yaml` (Milestone 1 output). Every entity in `sentinelone_ontology.yaml` validates against `data/schemas/ontology.schema.json`.

10 entities: **Alert** (18 attrs), **Vulnerability** (12 attrs), **InventoryItem**/endpoint (57 attrs), **Storyline** (indirectly reachable only, via `Alert.storylineId` — no dedicated tool), all `in_scope: true`; **Misconfiguration** (probed live, not confirmed populated in this tenant), **Policy**, **Incident**, **IdentitySecurity**, **CloudSecurity**, **AssetDiscoveryNetwork** — all `in_scope: false`, each citing the specific Milestone 1 finding that grounds it (no tool binding exists / no distinct concept / confirmed not licensed).

Attribute `population` labels (`always_present`/`usually_present`/`rarely_present`/`never_observed`) are real per-attribute non-null rates computed over each 25-item sample, not asserted from the API schema alone.

**The file carries `status: draft_pending_review`.** Per the build brief's Milestone 2 instruction, this is the human review checkpoint before freezing v1 — nothing downstream should treat it as frozen until a human sets `reviewed_by` on each entity. Re-run the builder script to refresh from a fresh sample; do not hand-edit population labels without re-deriving them from real data.
