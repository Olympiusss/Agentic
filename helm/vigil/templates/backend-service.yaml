apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentry-agentic.backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sentry-agentic.componentLabels" (dict "context" . "component" "backend") | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "sentry-agentic.componentSelectorLabels" (dict "context" . "component" "backend") | nindent 4 }}
