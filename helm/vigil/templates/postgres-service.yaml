{{- if and .Values.postgresql.enabled (not .Values.postgresql.bitnami.enabled) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentry-agentic.postgres.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sentry-agentic.componentLabels" (dict "context" . "component" "postgres") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: {{ .Values.postgresql.service.port }}
      targetPort: postgres
  selector:
    {{- include "sentry-agentic.componentSelectorLabels" (dict "context" . "component" "postgres") | nindent 4 }}
{{- end }}
